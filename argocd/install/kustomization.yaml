apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - https://raw.githubusercontent.com/argoproj/argo-cd/v2.12.0/manifests/install.yaml

patches:
  - path: patches/repo-server-helm-override.yaml
    target:
      kind: Deployment
      name: argocd-repo-server
